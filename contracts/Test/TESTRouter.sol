pragma solidity =0.8.9;



contract TESTRouter {
    function addLiquidity() external{
        //dumbie to get a vAMM or sAMM test token
    }

    function quoteRemoveLiquidity(address tokenA, address tokenB, bool stable, uint256 liquidity) external returns(uint256 amountA, uint256 amountB){
        return(liquidity, liquidity);
    }
}